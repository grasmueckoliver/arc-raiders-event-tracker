# Test profile: use H2 in-memory database
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=false
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# For tests, allow ddl-auto to update schema
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.show-sql=false

# Disable Metaforge cache for tests
metaforge.cache.enabled=false

# Disable actuator management port for tests (avoid port conflict)
management.server.port=-1
management.endpoints.web.exposure.include=
